name: info-contact-url
labels:
  - info
  - contact
  - ownership
  - openapi-info-object
  - openapi-contact-object
reason: |
  Just a contact name is often not enough, consumer will need a way to contact the API owner/support.
  This url could lead to the API details on the API portal, a contact form, a slack channel.
recommendedSeverity: error
recommended: false
sources:
  - path: ../../sources/raw/legacy/info-contact-url.raw-meta.yaml
