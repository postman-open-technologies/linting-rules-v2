name: http-authorized-methods
category: HTTP Ruleset 
labels:
  - http
reason: |
  HTTP provides a recommendation on the methods that should be supported within HTTP requests.
  This is applicable to paths, webhooks and callbacks.
recommendedSeverity: warn
recommended: true
sources:
  - path: ../../sources/raw/legacy/http-authorized-methods.raw-meta.yaml
